<Window x:Class="P_Layer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:P_Layer"
        xmlns:viewmodels="clr-namespace:P_Layer.ViewModels"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.DataContext>
        <viewmodels:MainWindowViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="202*"/>
            <ColumnDefinition Width="271*"/>
            <ColumnDefinition Width="323*"/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.ColumnSpan="1">
            <TextBox x:Name="TBx_WinCounter" Text="{Binding Path=WindowCount}" IsReadOnly="True"/>
            <Button x:Name="But_OpenBS_Win" Content="Blodsukker"  Command="{Binding CalcBMICommand, Mode=OneTime}" />
            <Button x:Name="But_OpenBP_Win" Content="Blodtryk" Command="{}" />
            <Button x:Name="But_OpenW_Win" Content="Vægt" Command="{}"  />
        </StackPanel>

    </Grid>
</Window>
